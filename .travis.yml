dist: trusty
sudo: required
language: generic

install: skip

script:
  - nvm --version
  - yarn --version
  - sudo rabbitmq-server --version

matrix:
  include:
    - group: stable
    - group: edge
  
