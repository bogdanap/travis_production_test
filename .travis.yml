sudo: required

language: java
  
script:
  - ls /usr/lib/jvm/
  - df -h
  - java -version
  - jdk_switcher use ibmjava8
  - java -version
  
matrix:
  include:
    - group: stable
      env: GROUP=stable
    - group: edge
      env: GROUP=edge


